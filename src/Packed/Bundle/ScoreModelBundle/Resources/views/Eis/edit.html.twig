<h4>{%trans%}Eis bewerken{%endtrans%}</h4>

<form action="{{ path('admin_eis_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit" class="btn btn-primary">{%trans%}Wijzigen{%endtrans%}</button>
    </p>
</form>


        <form action="{{ path('admin_eis_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit" class="btn btn-danger">Verwijder</button>
        </form>
